import { CostDirective } from "./cost.directive";
import { RateLimitDirective } from "./rate-limit.directive";

export const schemaDirectives = {
	cost: CostDirective,
	rateLimit: RateLimitDirective
};
